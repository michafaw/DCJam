///scrCreatePlantAt(x, y, plantInfo);

var xx = argument0;
var yy = argument1;
//var plantInfo = argument2; // Update with stats -- Micha TODO


instance_create_layer(xx, yy, "OutOfGround", objPlant);